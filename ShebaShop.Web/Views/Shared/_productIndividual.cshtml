@model ShebaShop.Web.Models.Product

    <div class="col-md-3 polaroid" style="float: left; border: 1px solid green; margin-right:5px;">
        <div>
            <h4>@Html.DisplayFor(model => model.ProductName)</h4>
            <hr />
            @{ 
                var imagePath = "/PostImages/" + Model.PhotoPath;
            }
            <img style="border:1px solid silver;" src="@imagePath" width="150" height="150" alt="No Image"/>

            @*<dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayFor(model => model.ProductName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Brand)
            </dd>
        </dl>*@
        </div>
        <div class="row">
            @*<a asp-action="Index" asp-controller="Basket" asp-route-id="@Model.Id">Add to ShoppingCart</a>*@
            @*<a asp-action="Index">Back to List</a>*@
            <form asp-action="Index" asp-controller="Basket" class="form-inline col-md-12" method="post" >
                <input type="hidden" name="pid" value="@Model.Id" />
                @*<input type="number" name="qty" min="1" max="100" value="1" width="30px;" class="form-control" />*@
                <button class="btn btn-primary btn-sm" type="submit">Add </button>
            </form>
        </div>
    </div>
